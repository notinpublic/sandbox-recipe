export type AlbumCardSetupEntry = {
    title: string;
    tracks: string;
    year: number;
    cover: string;
    alt: string;
  };

  export interface AlbumCardSetupProps {
    title: string;
    tracks: string;
    year: number;
    cover: string;
    alt: string;
}